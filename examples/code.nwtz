log(Test);

object = 6;

obj truc {
    name: "How are you today",
    age: String,
};

fn hello(arg){
    arg + 20
}

log(hello(5));

truc.age = object;

log(truc.age);

monTableau = [42, "texte", true, null];

log(monTableau[1]);

monTableau[1] = "Hey";
log(monTableau[1]);

monTableau[1] = test;
log(monTableau[1]);