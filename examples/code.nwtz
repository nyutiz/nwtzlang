log(Test);

object = 6;

obj truc {
    name: "How are you today",
    age: String,
};

fn hello(arg){
    fn say_hello(){
        log(arg);
    }
}

hello("HEY");

truc.age = object;

log(truc.age);

monTableau = [42, "texte", true, null];

log(monTableau[1]);

monTableau[1] = "Hey";
log(monTableau[1]);

monTableau[1] = test;
log(monTableau[1]);