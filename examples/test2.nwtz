obj truc {
    name: "How are you today",
    age: Function,
};

log(truc.age);

a : String = "éfdqgfqsfq";

obj truc {
    age: Integer,
};

truc.age = "Hey";

log(a);

say("FAaf");

fn say(x){
    log(x);
}

fn counter(init, max){
    for (i = init; i < max; i = i+1;){
        log(i);
    }
}

counter(0, 5);

fn read(name){
    log(_fs_read(name));
}

read(".gitignore");

a : String = "JE SUIS UN STR";
a : Boolean = true;
log(a);

b: Boolean = true;
b = false;
log(b);

c: Array = [54, "HELLO"];
log(c);

obj truc {
    age: "544654654654",
    name: String,
    c: c,
};


log(truc.c);

obj truc {
    truc: "AAA",
    fn hey(a){
        log(a);
    },
};

fn get(){
    time()
}

truc.hey("aaaa");

log(system.config);
gz = [10, 56, true, "AFAZF"];

log(gz[2]);

log(get());

//system.log(system.config);
truc.hey("a");
log( system.getValueType( truc.hey ) );

test=null;

fn __hello__ (){
    log(45454);
}

__hello__();

object = 6;

obj truc {
    name: "How are you today",
    age: String,
};

truc.age = object;

log(truc.age);

monTableau = [42, "texte", true, null];

log(monTableau[1]);

monTableau[1] = "Hey";
log(monTableau[1]);

monTableau[1] = test;
log(monTableau[1]);
log(monTableau);

input1: String = input();
log("Hello ", input1);

input2: String = input();
log("Hey ", input2);

log("Enter input : ");

dzgefs = input();

if dzgefs == "a" {
    log("aA");
} else {
    log("AFAFA ! ", dzgefs);
}

s = system.socket("localhost:8080");

log(s);
