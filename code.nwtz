object = 6;

obj truc {
    name: "How are you today",
    age: String,
};

truc.age = object;

log(truc.age);

monTableau = [42, "texte", true, null];

log(monTableau[1]);

monTableau[1] = "Hey";
log(monTableau[1]);
