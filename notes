## + => Fait, - =>  Pas Fait, -- => Pas prévu, +- => à moitié ##

Declarations de variables +
Appel variables +
False, True, Null +
Conditions +
Fonctions, Fonctions Natives +
Import de fichiers +
Integer, Float, String, "String"  +
Array +
Calculs, Priorités (% Ne fonctionne pas) +
Closures +
For +
ValueType définis explicitement par : +
Commentaires // +
Impl d'objets +
Meilleur Log log("Salut ", name) +
String condition if a == "b" +
Fonction Native getValueType pour comparer dans des conditions +
Thread (wait -> attend la fin de tous les threads) +
Changer obj truc {} par truc = {} +- (deux fonctionnent)


Implementation de self - // récupérer l'environnement de la fonction de l'objet et resolve dessus 
Boucle While -
Comparaisons Or, And -
Exposants ^ ou ** -
trier les propriétés dans l'ordre d'assignement -
gerer les errerus avec l'env et call_nwtz
Ajouter un type dans les Fonctions fn(a:Integer){} -


Remplacer obj par class --


/*
Idées

var locale a une fonction avec un _ devant

Pouvoir manipuler le thread avec a.stop(); etc ou a.join() qui appellerai le reste de la fonction dans le thread principal
*/


Bug
/*


a = 5;

aaaa();

log(a*a);

fn aaaa(){
    log("hey");
    log(a*a);
}

=> cargo run -- -r examples/another.nwtz
=>     Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.08s
=>      Running `target\debug\nwtzlang.exe -r examples/another.nwtz`
=> hey
=>
=> thread 'main' panicked at src\environment.rs:135:13:
=> Cannot resolve 'a' as it does not exist.
=> note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace
=> error: process didn't exit successfully: `target\debug\nwtzlang.exe -r examples/another.nwtz` (exit code: 101)

*/
